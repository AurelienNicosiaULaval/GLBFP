% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GLBFP.R
\name{GLBFP}
\alias{GLBFP}
\alias{print.GLBFP}
\title{General Linear Blend Frequency Polygon (GLBFP) estimator at a single point}
\usage{
GLBFP(
  x,
  data,
  b = compute_bi_optim(data, m = rep(1, ncol(data))),
  m = rep(1, ncol(data)),
  min_vals = apply(data, 2, min),
  max_vals = apply(data, 2, max)
)

\method{print}{GLBFP}(x, ...)
}
\arguments{
\item{x}{Object returned by \code{\link[=GLBFP]{GLBFP()}}.}

\item{data}{Numeric matrix or data frame of observations (\verb{n x d}).}

\item{b}{Positive numeric vector of bandwidths (length \code{d}).}

\item{m}{Positive integer vector of shifts (length \code{d}).}

\item{min_vals}{Numeric vector of lower grid bounds (length \code{d}).}

\item{max_vals}{Numeric vector of upper grid bounds (length \code{d}).}

\item{...}{Additional arguments (unused).}
}
\value{
A list with class \code{c("glbfp_fit", "GLBFP")} containing:
\code{x}, \code{estimation}, \code{sd}, \code{IC}, \code{b}, \code{m}, \code{method}, and \code{dimension}.
}
\description{
Computes the GLBFP density estimate at point \code{x}.
}
\details{
\code{GLBFP()} generalizes the linear blend frequency polygon workflow through the
positive integer shift vector \code{m}. Missing and non-finite values are not
accepted; remove or impute them before calling the estimator.
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(GLBFP)}: Print method for object of class \code{"GLBFP"}.

}}
\examples{
x <- c(200, 30)
b <- c(0.5, 0.5)
m <- c(1, 1)
GLBFP(x, ashua[, -3], b = b, m = m)

}
\references{
Scott, D. W. (1992). \emph{Multivariate Density Estimation: Theory, Practice, and
Visualization}. Wiley. doi:10.1002/9780470316849.

The complete methodological citation for GLBFP has not yet been verified in
this repository. Add it before using this help page as publication text.
}
\seealso{
\code{\link[=GLBFP_estimate]{GLBFP_estimate()}}, \code{\link[=ASH]{ASH()}}, \code{\link[=LBFP]{LBFP()}}, \code{\link[=compute_bi_optim]{compute_bi_optim()}}
}
