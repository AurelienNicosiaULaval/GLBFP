% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_Di.R
\name{compute_Di}
\alias{compute_Di}
\title{Leave-one-out self-support scores for grid density estimators}
\usage{
compute_Di(
  data,
  b = compute_bi_optim(data, m = rep(1, ncol(data))),
  m = rep(1, ncol(data)),
  estimator = c("GLBFP", "LBFP", "ASH"),
  min_vals = apply(data, 2, min),
  max_vals = apply(data, 2, max)
)
}
\arguments{
\item{data}{Numeric matrix or data frame of observations (\verb{n x d}).}

\item{b}{Positive numeric vector of bandwidths (length \code{d}).}

\item{m}{Positive integer vector of shifts (length \code{d}). Ignored for
\code{estimator = "LBFP"}, where \code{m = 1}.}

\item{estimator}{Character string. One of \code{"GLBFP"}, \code{"LBFP"}, or \code{"ASH"}.}

\item{min_vals}{Numeric vector of lower grid bounds (length \code{d}).}

\item{max_vals}{Numeric vector of upper grid bounds (length \code{d}).}
}
\value{
A list with class \code{"glbfp_di"} containing the score vector \code{D},
fitted densities, leave-one-out densities, self-weights, and metadata.
}
\description{
Computes the fixed-grid leave-one-out score
\deqn{D_i = 1 - \widehat f_{(-i)}(X_i) / \widehat f(X_i)}
for observations in \code{data}. The grid, bandwidths, and estimator parameters
are held fixed when the contribution of observation \code{i} is removed.
}
\examples{
x <- as.matrix(ashua[seq_len(80), -3])
b <- c(0.5, 0.5)
out <- compute_Di(x, b = b, m = c(1, 1), estimator = "GLBFP")
summary(out)

}
